export enum SocketIO {
  SocketTag = "message"
}
export enum SocketIOMessage {
  Typing = "Typing",
  Connected = "Connected"
}

export enum MessageClass {
  NewMessageClass = "newMessage"
}
export enum SocketUrl {
  url = "http://localhost:3000"
}
